#18. Given a numeric vector, write R code to create a new vector where all values less than the mean of the vector are
#replaced with NA.

num_vec <- c(5, 15, 25, 8, 18)
mean_val <- mean(num_vec)

num_vec[num_vec < mean_val] <- NA
print(num_vec)

#OUTPUT
#[1] NA 15 25 NA 18
