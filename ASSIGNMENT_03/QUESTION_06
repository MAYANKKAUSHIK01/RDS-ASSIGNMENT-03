#6. Write R code that calculates the sum of all TRUE values in a given logical vector (using their numeric property
#where TRUE = 1 and FALSE = 0).

log_vec <- c(TRUE, FALSE, TRUE, TRUE, FALSE)
sum_true <- sum(log_vec)
print(sum_true)

#OUTPUT
#[1] 3
